{
  "name": "resistor",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "sync": "npx cap sync android",
    "build-apk": "npm run build && npx cap sync android && cd android && ./gradlew assembleDebug",
    "clean": "rm -rf dist android/app/build"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  }
}
